import { expect } from 'chai';

describe('ScatterAuction', () => {
	
	before(async () => {
		// This will be executed as an constructor,
		// all computations before testing here
	})
	
	it('trivial test', async () => {
		expect(1).to.equal(1);
	})

})
